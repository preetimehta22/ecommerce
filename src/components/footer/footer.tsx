import { alpha, InputBase } from "@mui/material";
import * as React from "react";
import * as FS from "./footer.styled";


export const Footer: React.FC = () => {

  return (
      <FS.Footer>
        <img src="/images/logo.svg" />
      </FS.Footer>
  );
};
